<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sangeng.mapper.CommentMapper">
    <update id="updateUserCommentMessageRead">
        update
            `sg_comment`
        set `read` = 1
        WHERE
            `read` = 0
          AND del_flag = 0
          AND create_by != #{userId}
          AND article_id IN (
            SELECT
            id
            FROM
            `sg_article`
            WHERE
            user_id = #{userId})
    </update>
    <update id="updateRead">
        update
            `sg_comment`
        set `read` = 1
        where id = #{commentId}
    </update>


    <select id="getUnReadCommentList" resultType="com.sangeng.domain.entity.Comment">
        SELECT
            *
        FROM
            `sg_comment`
        WHERE `read` = 0
          AND del_flag = 0
          AND create_by != #{userId}
        AND article_id IN (
        SELECT
            id
        FROM
            `sg_article`
        WHERE
            user_id = #{userId})
    </select>
    <select id="getUnReadCommentCount" resultType="java.lang.Integer">
        SELECT
            count(1)
        FROM
            `sg_comment`
        WHERE
            `read` = 0
          AND del_flag = 0
          AND create_by != #{userId}
        AND article_id IN (
        SELECT
            id
        FROM
            `sg_article`
    WHERE
        user_id = #{userId})
    </select>
    <select id="getRecentUnReadCommentList" resultType="com.sangeng.domain.entity.Comment">
        SELECT
            *
        FROM
            `sg_comment`
        WHERE
            del_flag = 0
          AND create_by != #{userId}
          AND article_id IN (
            SELECT
            id
            FROM
            `sg_article`
            WHERE
            user_id = #{userId})
        order by create_time desc limit 5;
    </select>
</mapper>
